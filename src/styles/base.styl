@import 'variable.styl'

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  width: 100%;
  height: 100%;
}

ul {
  list-style: none;
}

button {
  font-size: 14px;
  border: none;
  border-radius: 4px;
  line-height: 1;
  outline: none;
  cursor: pointer;
}